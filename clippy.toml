msrv = "1.74.0"
# plan API returns Self as an error type for an large-ish enum
large-error-threshold = 256
disallowed-types = [
    { path = "bitcoin::Sequence", reason = "unstable 0.32 sequence, import bitcoin::compat::Sequence. Allowed sites are compat boundaries and legacy struct fillers" },
]
